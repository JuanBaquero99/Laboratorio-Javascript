// includes() with numbers

const number = [1,2,3,4,5]

const result = number.includes(3)
console.log(result)

const result2 = number.includes(8)
console.log(result2)

//Nos devuelve en respuesta boleana si se incluye o no el valor que se pregunta por ejemplo en el array

//indexOf()

const index = number.indexOf(3)
console.log(index)
const index2 = number.indexOf(8)
console.log(index2)

// podemos ver que nos indica el indice donde se encuentra el numero
//Cuando el número no tiene indice devuelve -1
