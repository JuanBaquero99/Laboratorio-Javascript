/*

array.forEach((item)) => {
    codigo a ejecutar
})

*/

let list = ['eat', 'sleep', 'code', 'repeat'];

list.forEach((item) => {
    console.log(item);
});

//En lista se usa la función forEach, es decir por cada elemento que existe en la lista, ejecutar el siguiente codigo
//Que eso que se ejecuta imprima item
